#!/bin/bash
TIMEOUT=${SAVE_TIMEOUT_SECONDS:-30}

echo "[save-server] sending SaveWorld via RCON (timeout: ${TIMEOUT}s)"
/scripts/send-command "SaveWorld" 2>/dev/null || true
sleep "$TIMEOUT"
echo "[save-server] save complete"
