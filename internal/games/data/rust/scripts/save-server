#!/bin/bash
TIMEOUT=${SAVE_TIMEOUT_SECONDS:-15}

echo "[save-server] sending server.save via RCON (timeout: ${TIMEOUT}s)"
/scripts/send-command "server.save" 2>/dev/null || true
sleep "$TIMEOUT"
echo "[save-server] save complete"
