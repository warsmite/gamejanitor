#!/bin/bash
set -e

echo "[update-server] updating Garry's Mod dedicated server via SteamCMD"
steamcmd.sh \
    +force_install_dir /data/server \
    +login anonymous \
    +app_update 4020 validate \
    +quit

echo "[update-server] update complete"
